# Project URL from the Supabase dashboard (Settings → API).
SUPABASE_URL=

# Service-role key (Settings → API). Server-only secret: bypasses RLS.
# Never expose this to the frontend or commit a real value.
SUPABASE_SERVICE_ROLE_KEY=
